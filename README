Installation:

	git clone git://github.com/sbennett/dotfiles.git ~/.dotfiles

Create symlinks:

	~/.dotfiles/install

Commit changes back to git

	git add <whatever>
	git commit -m "Changed whatever"
	git push origin master

